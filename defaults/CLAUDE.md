# Global Rules

For non-trivial tasks:
- Read relevant files before editing.
- Search for usages, related types, and tests before changing behavior.
- State a short plan before large, risky, or multi-file edits.
- Verify the result before claiming the task is done.
- If requirements are ambiguous, ask instead of guessing.

Prefer precise, minimal changes over broad speculative refactors.
